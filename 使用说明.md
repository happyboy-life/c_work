# 校园二手图书交易管理系统 — 使用说明

---

## 一、快速入门

### 1.1 初始化模拟数据

在首次启动系统前，建议先运行种子数据初始化程序，为数据库填充模拟图书和用户数据：

```bash
# 方式一：C 可执行程序（推荐，带进度和验证）
gcc -o seed_data.exe seed_data.c -lsqlite3 -lws2_32
seed_data.exe

# 方式二：SQL 脚本
sqlite3 bookstore.db ".read seed_data.sql"
```

> 初始化程序会创建 8 本模拟图书和 8 位模拟用户，用于系统功能测试。详细数据说明请参阅 `模拟图书数据说明.md`。

### 1.2 启动系统

#### 方式：一键启动（推荐）
直接**双击 `run.bat`**，脚本会自动编译程序并启动服务器，同时自动打开浏览器。

### 1.3 访问系统
服务器启动后，打开浏览器访问：
```
http://localhost:8080
```
首页即为**登录页面**。

---

## 二、用户登录（登录即注册）

本系统采用**登录即注册**机制，无需单独注册账号：

1. 输入**用户名**和**密码**
2. 点击"登录"
3. 若用户名不存在，系统将**自动创建新账号**并登录
4. 若用户名存在但密码错误，提示"用户名或密码错误"
5. 登录成功后进入**图书浏览首页**

### 2.1 模拟用户（可直接登录测试）

以下为种子数据创建的模拟用户，均可用于登录测试：

| 用户名 | 密码 | 姓名 | 班级 |
|--------|------|------|------|
| bookworm_lee | 123456 | 书虫小李 | 中文系2022级 |
| programmer_wang | 123456 | 程序员小王 | 计算机系2021级 |
| history_fan | 123456 | 历史爱好者 | 历史系2022级 |
| econ_master | 123456 | 经济学霸 | 经济学院2022级 |
| exam_warrior | 123456 | 考研党 | 数学系2020级 |
| art_youth | 123456 | 文艺青年 | 文学院2022级 |
| science_geek | 123456 | 科学达人 | 物理系2023级 |
| philosophy_lover | 123456 | 哲学思考者 | 哲学系2021级 |

### 2.2 完善个人资料
登录后，点击导航栏中的"个人资料"（或访问 `/mydata.html`），填写：
- 真实姓名
- 学号
- 班级
- 头像链接（可选）

---

## 三、功能页面导航

| 页面 | 访问方式 | 功能说明 |
|------|---------|---------|
| **首页/图书浏览** | 点击"首页"或访问 `/` | 浏览所有在售图书，支持搜索筛选 |
| **图书详情** | 点击任意图书卡片 | 查看图书完整信息，支持收藏/购买 |
| **发布图书** | 点击"发布"或访问 `/publish.html` | 卖家发布二手图书信息 |
| **个人中心** | 点击"我的"或访问 `/mine.html` | 查看已发布/已购买/已收藏图书 |
| **个人资料** | 点击头像或访问 `/mydata.html` | 编辑个人资料信息 |
| **购买确认** | 在详情页点击"购买" | 确认购买信息并提交 |

---

## 四、核心操作指南

### 4.1 发布图书（卖家）
1. 登录后进入"发布图书"页面
2. 填写图书信息：
   - **书名**（必填）
   - **作者**（必填）
   - **ISBN号**
   - **出版社**
   - **分类**（如：教材、文学、计算机等）
   - **成色**（全新 / 良好 / 一般 / 较差）
   - **价格**（单位：元）
   - **库存数量**
   - **图片URL**（可选，填写网络图片链接）
3. 点击"发布"按钮
4. 发布成功后图书将出现在首页列表中

### 4.2 搜索图书
在首页搜索栏中，支持以下筛选条件：
- **关键词**：按书名或作者模糊搜索
- **作者**：精确搜索作者名
- **ISBN**：按ISBN号搜索
- **价格区间**：设置最低价和最高价
- **分类**：按图书分类筛选
- **状态**：筛选在售/已售/已下架图书

### 4.3 收藏图书
1. 在图书详情页点击"收藏"按钮
2. 收藏成功后可在"个人中心 → 我的收藏"中查看
3. 重复收藏同一图书会提示"已收藏过"

### 4.4 购买图书
1. 在图书详情页点击"购买"按钮
2. **第一次确认弹窗**：填写买家信息
   - **买家姓名**（必填）
   - **联系电话**（必填）
   - **学号**（必填）
3. 点击"确认购买"后弹出**二次提醒弹窗**
   - 显示书名、价格等确认信息
   - 提示"购买后请主动联系卖家完成线下交易"
   - 需再次点击"确定购买"才能完成
4. 购买成功后：
   - 图书库存自动减 1
   - 库存为 0 时自动变为"已售"状态
   - 交易记录保存到"我的购买"列表中
   - **自动弹出卖家QQ联系方式**，方便联系交易
   - 系统自动记录：买家姓名、电话、学号、购买时间

### 4.5 下架图书
仅卖家可下架自己发布的图书，下架时需验证身份：
1. 进入"个人中心 → 我发布的"
2. 找到要下架的图书，点击"**下架**"按钮
3. 弹出**学号验证弹窗**，需输入发布者的学号
4. 学号与当前登录用户的学号匹配后才可确认下架
5. 验证失败（学号不匹配）则拒绝下架
6. 下架后该图书不再在首页显示

---

## 五、个人中心功能

登录后点击"我的"进入个人中心，包含三个标签页：

| 标签页 | 显示内容 |
|--------|---------|
| **我发布的** | 当前用户发布的所有图书（含在售/已售/已下架） |
| **我购买的** | 当前用户购买的所有图书记录 |
| **我的收藏** | 当前用户收藏的所有图书列表 |

---

## 六、数据存储说明

### 6.1 数据库文件
系统数据存储在以下文件中（均位于程序同目录 `c_work/`）：

| 文件 | 格式 | 说明 |
|------|------|------|
| `bookstore.db` | SQLite3 数据库 | 主数据库文件，首次运行时自动创建 |
| `bookstore.db-shm` | WAL 缓存 | SQLite WAL 模式共享内存文件（自动生成） |
| `bookstore.db-wal` | WAL 缓存 | SQLite WAL 模式预写日志（自动生成） |
| `books.dat` | 二进制快照 | 图书记录备份文件，每次发布/购买/下架后自动保存 |
| `users.dat` | 二进制快照 | 用户记录备份文件，每次登录/注册后自动保存 |

### 6.2 种子数据文件

| 文件 | 说明 |
|------|------|
| `seed_data.c` | 独立 C 数据初始化程序（含建表、用户、图书，带进度和验证） |
| `seed_data.sql` | SQL 种子数据脚本（可通过 sqlite3 直接执行） |
| `模拟图书数据说明.md` | 模拟图书数据详细说明文档 |

### 6.3 数据加载流程
```
服务器启动
  ├─ 1. 清理 WAL 残留文件，确保 SQLite 正常打开（最多重试3次）
  ├─ 2. 优先从 books.dat 加载图书记录到内存链表
  │     └─ 若 books.dat 不存在或为空 → 从 SQLite 加载并生成 books.dat
  ├─ 3. 优先从 users.dat 加载用户记录到内存链表
  │     └─ 若 users.dat 不存在或为空 → 从 SQLite 加载并生成 users.dat
  └─ 4. 启动 HTTP 服务，等待请求
```

### 6.4 数据保存时机
| 操作 | SQLite | books.dat | users.dat |
|------|--------|-----------|-----------|
| 发布图书 | ✅ 插入 | ✅ 自动保存 | — |
| 购买图书 | ✅ 更新 | ✅ 自动保存 | — |
| 下架图书 | ✅ 更新 | ✅ 自动保存 | — |
| 用户登录/注册 | ✅ 插入 | — | ✅ 自动保存 |
| 服务器退出 (Ctrl+C) | ✅ checkpoint | ✅ 自动保存 | ✅ 自动保存 |

### 6.5 安全关闭服务器
按 **Ctrl+C** 正常退出，系统会自动保存所有数据并执行 SQLite checkpoint。**请勿直接关闭窗口或强制结束进程**，否则可能导致 WAL 文件残留，影响下次启动。

---

## 七、常见问题

### Q1：双击 run.bat 无反应或报错？
**A**：请确保已安装 GCC 编译器和 SQLite3 库。推荐安装 [MSYS2](https://www.msys2.org/)，安装后运行：
```bash
pacman -S mingw-w64-x86_64-gcc mingw-w64-x86_64-sqlite3
```

### Q2：浏览器无法访问 localhost:8080？
**A**：
1. 确认 server.exe 已启动（命令行窗口显示"服务器启动中"）
2. 检查 8080 端口是否被其他程序占用
3. 检查防火墙是否阻止了程序

### Q3：登录时提示"用户名或密码错误"？
**A**：该用户名已存在但输入的密码不正确，请检查密码后重试。

### Q4：购买时提示"库存不足或已下架"？
**A**：该图书可能已被他人购买完毕，或被卖家下架。

### Q5：下架图书时提示"无权操作此图书"？
**A**：只能下架自己发布的图书，不能下架他人发布的图书。

### Q6：数据库文件在哪里？
**A**：所有数据文件均在程序同目录 `c_work/` 下：
- `bookstore.db` — SQLite 主数据库
- `books.dat` — 图书记录备份
- `users.dat` — 用户记录备份
配套的 `.db-shm` 和 `.db-wal` 是 WAL 缓存文件，请勿手动删除。

### Q7：如何手动编译运行？
**A**：在项目目录中执行以下命令：
```bash
gcc server.c simple_database.c data_persistence.c -o server.exe -lsqlite3 -lws2_32 -O2 -Wall
server.exe
```
> 注意：必须同时编译 `data_persistence.c`，否则会因缺少序列化函数而链接失败。

### Q8：如何安全关闭服务器？
**A**：在服务器命令行窗口按 **Ctrl+C**，系统会自动保存所有数据到 `books.dat`、`users.dat` 和 `bookstore.db`，然后安全退出。请勿直接关闭窗口或强制结束进程。

### Q9：启动后首页没有图书？
**A**：数据库尚未初始化模拟数据，请先运行种子数据程序：
```bash
gcc -o seed_data.exe seed_data.c -lsqlite3 -lws2_32
seed_data.exe
```
或使用 SQL 脚本：
```bash
sqlite3 bookstore.db ".read seed_data.sql"
```

---

## 八、项目结构

```
c_work/
├── server.c              # HTTP服务器主程序
├── simple_database.c     # 数据库操作层（SQLite3 + 链表缓存）
├── database.h            # 数据结构定义与接口声明
├── data_persistence.c    # 数据持久化模块（序列化/反序列化）
├── data_persistence.h    # 数据持久化模块头文件
├── welcome.c             # 控制台欢迎/测试程序
├── test.c                # 独立测试/数据填充工具
├── seed_data.c           # 种子数据初始化程序（独立可执行）
├── seed_data.sql         # 种子数据 SQL 脚本
├── run.bat               # 一键编译启动脚本
├── bookstore.db          # SQLite3 数据库文件
├── bookstore.db-shm      # WAL 模式缓存（自动生成）
├── bookstore.db-wal      # WAL 模式缓存（自动生成）
├── books.dat             # 图书数据备份文件（自动生成）
├── users.dat             # 用户数据备份文件（自动生成）
├── login.html            # 前端页面文件
├── goods.html            # 前端页面文件
├── publish.html          # 前端页面文件
├── mine.html             # 前端页面文件
├── mydata.html           # 前端页面文件
├── detail.html           # 前端页面文件
├── 使用说明.md            # 本文件
├── 模拟图书数据说明.md    # 模拟数据详细说明
└── README.txt            # 项目说明文档
```

---

*文档版本：v4.0 | 更新日期：2026年5月*