# 校园二手图书交易管理系统 — 使用说明

---

## 一、快速入门

### 1.1 启动系统

#### 方式：一键启动（推荐）
直接**双击 `run.bat`**，脚本会自动编译程序并启动服务器，同时自动打开浏览器。

### 1.2 访问系统
服务器启动后，打开浏览器访问：
```
http://localhost:8080
```
首页即为**登录/注册页面**。

---

## 二、用户注册与登录

### 2.1 注册账号
1. 在登录页点击"注册"按钮
2. 输入**用户名**和**密码**
3. 注册成功后自动跳转到登录页

### 2.2 登录系统
1. 输入已注册的用户名和密码
2. 点击"登录"
3. 登录成功后进入**图书浏览首页**

### 2.3 完善个人资料
登录后，点击导航栏中的"个人资料"（或访问 `/mydata.html`），填写：
- 真实姓名
- 学号
- 班级
- 头像链接（可选）

---

## 三、功能页面导航

| 页面 | 访问方式 | 功能说明 |
|------|---------|---------|
| **首页/图书浏览** | 点击"首页"或访问 `/` | 浏览所有在售图书，支持搜索筛选 |
| **图书详情** | 点击任意图书卡片 | 查看图书完整信息，支持收藏/购买 |
| **发布图书** | 点击"发布"或访问 `/publish.html` | 卖家发布二手图书信息 |
| **个人中心** | 点击"我的"或访问 `/mine.html` | 查看已发布/已购买/已收藏图书 |
| **个人资料** | 点击头像或访问 `/mydata.html` | 编辑个人资料信息 |
| **购买确认** | 在详情页点击"购买" | 确认购买信息并提交 |

---

## 四、核心操作指南

### 4.1 发布图书（卖家）
1. 登录后进入"发布图书"页面
2. 填写图书信息：
   - **书名**（必填）
   - **作者**（必填）
   - **ISBN号**
   - **出版社**
   - **分类**（如：教材、文学、计算机等）
   - **成色**（全新 / 良好 / 一般 / 较差）
   - **价格**（单位：元）
   - **库存数量**
   - **图片URL**（可选，填写网络图片链接）
3. 点击"发布"按钮
4. 发布成功后图书将出现在首页列表中

### 4.2 搜索图书
在首页搜索栏中，支持以下筛选条件：
- **关键词**：按书名或作者模糊搜索
- **作者**：精确搜索作者名
- **ISBN**：按ISBN号搜索
- **价格区间**：设置最低价和最高价
- **分类**：按图书分类筛选
- **状态**：筛选在售/已售/已下架图书

### 4.3 收藏图书
1. 在图书详情页点击"收藏"按钮
2. 收藏成功后可在"个人中心 → 我的收藏"中查看
3. 重复收藏同一图书会提示"已收藏过"

### 4.4 购买图书
1. 在图书详情页点击"购买"按钮
2. 进入购买确认页面
3. 填写**买家姓名**
4. 确认购买
5. 购买成功后：
   - 图书库存自动减 1
   - 库存为 0 时自动变为"已售"状态
   - 交易记录保存到"我的购买"列表中

### 4.5 下架图书
仅卖家可下架自己发布的图书：
1. 进入"个人中心 → 我发布的"
2. 找到要下架的图书
3. 点击"下架"按钮
4. 下架后该图书不再在首页显示

---

## 五、个人中心功能

登录后点击"我的"进入个人中心，包含三个标签页：

| 标签页 | 显示内容 |
|--------|---------|
| **我发布的** | 当前用户发布的所有图书（含在售/已售/已下架） |
| **我购买的** | 当前用户购买的所有图书记录 |
| **我的收藏** | 当前用户收藏的所有图书列表 |

---

## 六、常见问题

### Q1：双击 run.bat 无反应或报错？
**A**：请确保已安装 GCC 编译器和 SQLite3 库。推荐安装 [MSYS2](https://www.msys2.org/)，安装后运行：
```bash
pacman -S mingw-w64-x86_64-gcc mingw-w64-x86_64-sqlite3
```

### Q2：浏览器无法访问 localhost:8080？
**A**：
1. 确认 server.exe 已启动（命令行窗口显示"服务器启动中"）
2. 检查 8080 端口是否被其他程序占用
3. 检查防火墙是否阻止了程序

### Q3：注册时提示"用户名已存在"？
**A**：该用户名已被注册，请更换用户名后重试。

### Q4：购买时提示"库存不足或已下架"？
**A**：该图书可能已被他人购买完毕，或被卖家下架。

### Q5：下架图书时提示"无权操作此图书"？
**A**：只能下架自己发布的图书，不能下架他人发布的图书。

### Q6：数据库文件在哪里？
**A**：数据库文件 `bookstore.db` 在程序同目录下，首次运行服务器时自动创建。配套的 `bookstore.db-shm` 和 `bookstore.db-wal` 文件是 SQLite WAL 模式的缓存文件，请勿手动删除。

### Q7：如何手动编译运行？
**A**：在项目目录中执行以下命令：
```bash
gcc server.c simple_database.c -o server.exe -lsqlite3 -lws2_32 -O2 -Wall
server.exe
```

---

## 七、项目结构

```
c_work/
├── server.c              # HTTP服务器主程序
├── simple_database.c     # 数据库操作层（SQLite3 + 链表缓存）
├── database.h            # 数据结构定义与接口声明
├── welcome.c             # 控制台欢迎/测试程序
├── test.c                # 独立测试/数据填充工具
├── run.bat               # 一键编译启动脚本
├── bookstore.db          # SQLite3 数据库文件
├── bookstore.db-shm      # WAL 模式缓存（自动生成）
├── bookstore.db-wal      # WAL 模式缓存（自动生成）
├── login.html            # 前端页面文件
├── goods.html            # 前端页面文件
├── publish.html          # 前端页面文件
├── mine.html             # 前端页面文件
├── mydata.html           # 前端页面文件
├── detail.html           # 前端页面文件
├── 使用说明.md            # 本文件
└── README.txt            # 项目说明文档
```

---

*文档版本：v2.0 | 更新日期：2026年5月*